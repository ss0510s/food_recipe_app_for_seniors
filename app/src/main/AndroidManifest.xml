<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="이 요리 어뗘"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".EffectActivity2"
            android:parentActivityName=".EffectActivity"
            ></activity>
        <activity
            android:name=".FoodActivity"
            android:parentActivityName=".MaterialActivity" />
        <activity android:name=".ItemActivity"/>
        <activity
            android:name=".CheckActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".FamousActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MaterialActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EffectActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>